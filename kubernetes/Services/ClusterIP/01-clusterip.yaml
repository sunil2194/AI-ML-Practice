apiVersion: v1
kind: Service
metadata:
  name: web-clusterip
  namespace: k8s-services-lab
spec:
  type: ClusterIP
  selector:
    app: web-demo
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80  
