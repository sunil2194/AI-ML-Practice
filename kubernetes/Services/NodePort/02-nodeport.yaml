apiVersion: v1
kind: Service
metadata:
  name: web-nodeport
  namespace: k8s-services-lab
spec:
  type: NodePort
  selector:
    app: web-demo
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
