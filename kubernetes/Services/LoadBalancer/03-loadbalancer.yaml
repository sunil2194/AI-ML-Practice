apiVersion: v1
kind: Service
metadata:
  name: web-loadbalancer 
  namespace: k8s-services-lab
spec:
  type: LoadBalancer
  selector:
    app: web-demo
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80  
